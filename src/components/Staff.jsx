import { Inbox, LogOut, Trash2 } from 'lucide-react';
import SectionTitle from './SectionTitle';
export default function Staff({apps,onLogout,onClear}){return <><div className="titleAction"><SectionTitle eyebrow="Staff area" title="Admission requests"/><button className="ghost" onClick={onLogout}><LogOut size={16}/>Log out</button></div>{apps.length===0?<div className="card empty"><Inbox size={32}/><p>No requests yet. New admission requests will appear here.</p></div>:<div className="newsList">{[...apps].sort((a,b)=>b.date.localeCompare(a.date)).map(a=><article className="card request" key={a.id}><div className="requestTop"><span>{a.grade}</span><small>{a.date.slice(0,10)}</small></div><h3>{a.student}</h3><p><b>Parent:</b> {a.parent}</p>{a.phone&&<p><b>Phone:</b> {a.phone}</p>}{a.email&&<p><b>Email:</b> {a.email}</p>}{a.location&&<p><b>Location:</b> {a.location}</p>}{a.message&&<em>“{a.message}”</em>}</article>)}</div>}{apps.length>0&&<button className="clearBtn" onClick={onClear}><Trash2 size={16}/>Clear all requests</button>}</>}
